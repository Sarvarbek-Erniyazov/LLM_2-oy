# Course views of the model ladder
Derived views of the real ladder configuration (`configs/model/ladder_v0.yaml`, `configs/train/joint_v0.yaml`) for the course's Lesson 7.
Not run configs: nothing trains from these files. Every value is pinned to its source by `tests/model/test_course_design.py`.
